import React from "react";

const ProgressBar = () => (
    <div className="container">
        <progress className="progress is-large is-dark">Loading</progress>
    </div>
);

export default ProgressBar;